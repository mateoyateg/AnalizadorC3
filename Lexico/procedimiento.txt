CREATE PROCEDURE funcion @ContactName nvarchar(30), @City nvarchar(10)
BEGIN
CREATE TABLE Customers {CustomerId BigInt, ContactName varchar(30), City varchar(30)};
UPDATE Customers SET ContactName='AlfredSchmidt',City='Frankfurt' WHERE CustomerID=1;
INSERT INTO Customers (CustomerName,City,Country) VALUES ('Cardinal','Stavanger','Norway');
DELETE FROM Customers WHERE CustomerName='Alfreds Futterkiste';
SELECT Customers.A FROM Customers WHERE Customers.Id = 1;
END
